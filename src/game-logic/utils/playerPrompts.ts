import { Player } from "../types/player";

export function promptPlayerChoice(player: Player, options: { id: string; label: string }[]) {
    // This is going to be an all encompassing function that will handle all player prompts. I'm not sure if the UI will stay the same, but for now, this will be the only place to handle player prompts.

    // 1. Choosing production: When a Card/Wonder stage 
    
}

